input GameInput {
  fen: String
  move: String
}

type Game {
  fen: String
  ascii: String
  moves: [String]
  inCheck: Boolean
  history: [String]
}

type Query {
  getGame(fen: String): Game
}

type Mutation {
  updateGame(input: GameInput): Game
}
