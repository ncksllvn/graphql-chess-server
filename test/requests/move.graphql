mutation {
  move(input: {
    fen: "rnbqkbnr/pppppppp/8/8/8/8/PPPPPPPP/RNBQKBNR w KQkq - 0 1",
    move: {
      from: e2,
      to: e4
    }
  }) {
    ascii
    analysis {
      bestMove {
        from
        to
        flags
      }
    }
    board {
      a { type color }
      b { type color }
      c { type color }
      d { type color }
      e { type color }
      f { type color }
      g { type color }
      h { type color }
    }
    fen
    gameOver
    inCheck
    inCheckmate
    inDraw
    inStalemate
    insufficientMaterial
    inThreefoldRepetition
    moves {
      color
      from
      to
      promotion
    }
    turn
  }
}
